# Backend (Express + MongoDB)

## Requirements
- Node.js (LTS)
- MongoDB (local) OR MongoDB Atlas

## Setup
Create `backend/.env`:

```env
PORT=4000
MONGO_URI=mongodb://127.0.0.1:27017/eventdb
JWT_SECRET=superlongrandomsecret123